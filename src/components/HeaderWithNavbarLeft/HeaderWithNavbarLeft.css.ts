import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  leftHeader: {
    flexDirection: "row",
    gap: 9,
  },
  textTitle: {
    fontSize: 20,
    marginLeft: 24,
    fontWeight: "500",
  },
});

export default styles;
