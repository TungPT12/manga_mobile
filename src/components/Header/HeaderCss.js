import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    headerContainer: {
        zIndex: 1,
        position: 'relative',
        height: 50,
        paddingHorizontal: 8
        // marginBottom: 4
    }
})

export default style